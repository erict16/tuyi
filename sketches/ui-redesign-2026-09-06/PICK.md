# PICK: A

红绿灯只画一排，设置是整页（工具栏换成「完成」），不是现在的参数弹层。空状态写「还没打开图纸，点左上角打开」，文案走数字、尺寸 / 网上翻译 / 图纸上只留译文。检查更新在底栏转一下、180ms 淡入，不另开窗。还是侧栏图纸 + 文字表 + 写回，不是落地页。

色板和 `.win .tb .side .item .table .foot` 跟现有 `App.css` 对得上，不用 Tailwind，也不用换暗色、奶油纸、Finder 路径条或假窗口框。
