# Project memory

- 2026-09-06: v0.1.4. Packaged app crashed on launch: PyInstaller excluded `uvicorn.supervisors.statreload` and `watchfilesreload`; uvicorn import is try/except so analysis missed the fallback and `import uvicorn` died. Both Windows and macOS specs now hiddenimport `uvicorn.supervisors.statreload` (and basereload/multiprocess) and no longer exclude those modules. `watchfiles` still excluded. macOS DMG is no longer `-srcfolder Tuyi.app` (that dumped bundle guts / no Applications). Staging is `Tuyi.app` + `/Applications` symlink, HFS+ UDRW, Finder icon layout, UDZO. README uses `docs/icons/app-rounded.png` (squircle mask of `app.png`; square `app.png` still feeds icns). Tag `v0.1.4` to ship. Do not bundle ODA.
- 2026-09-02: Landing download is two cards. Windows: 10/11 x64, Setup size, WebView2, unsigned click-path. Mac: Apple 芯片 vs Intel picker (not the same DMG); Intel URL empty until a x86_64 DMG exists. Latest release assets override hardcoded v0.1.3 URLs. 三步 + ODA note + 常见问题. Do not invent an Intel file.
- 2026-09-02: v0.1.3. Tag `v0.1.3`. Windows `Tuyi_v0.1.3_Setup.exe` + zip; Mac `Tuyi_v0.1.3_macOS_arm64.dmg` (Intel macos-13 optional). Landing DL.win/DL.mac point at those names. CI sets PYTHONUTF8 so Windows cp1252 cannot crash `safe_log`/CLI. Features from #5+#6. No ODA. No signing unless secrets exist.
- 2026-09-02: Remaining product holes. 批量导入 also Excel (.xlsx via openpyxl). 参数: 块内文字, 还原目录结构, 无 ODA 时改写 DXF (output DWG without ODA → DXF; input DWG still needs ODA). ACAD_TABLE write also copies group 1/303 twins of 302 and bumps revision so AutoCAD regen has source text; preview *T still synced. Cannot prove AutoCAD itself. CLI `--style`. macOS CI matrix arm64 + macos-13 Intel (Intel continue-on-error). Releases write appcast.xml + latest.yml. Authenticode / Developer ID only if secrets exist; unsigned zip apply stays. No ODA in payload.
- 2026-09-02: v0.1 leftovers. 批量导入 is CSV table fill-back (not Excel, not glossary import): 导出表格 / 导入表格 / 写回 / 全部写回. Columns `file,handle,field,source,target,layer,type`; also accepts 原文,译文. Handle+field first, then exact source. `via=edit` so MTEXT codes are not replayed. 参数: 我的术语 edit/delete/export + 本任务使用术语表 (`use_glossary`). PDF of source follows 版式; PDF of write-back stays 纯译文 (drawing already stamped). 批量导出: 暂停/继续/停止/重试. In-app zip apply stays (no Setup.exe). Do not touch docs/icons/.
- 2026-08-31: Three bugfixes on `main` (no new branch/PR). Closing 参数 no longer re-extracts unless 标注/表格/块属性/模型·图纸空间/图层/过滤 actually changed — so in-progress 译文 stay. `output_path_for` strips `.dxf`/`.dwg` (no `out.dxf.dxf`) and refuses to overwrite the source path (`resolve`/`samefile`); CLI `-o drawing.dxf` writes a prefixed+timestamp sibling beside it. Dropped-file cache is `~/.tuyi_dropped_files`; one-time move from `~/cad_translator_dropped_files`. Suite 177 ok + 1 pre-existing Linux error (`test_windows_keeps_edgechromium_and_exe_candidates`, shutil.which + patched win32). Do not touch 500words / huaming-en / UI redesign / installer.
- 2026-08-28: CLI debug. Windows ODA lookup now uses PATH (`shutil.which`), so `C:\Apps\ODAFileConverter` is found. `dwglot --version` works. `--provider` optional. CLI write-back with zero glossary hits and no engine says `没有可写回的译文` (not 勾选). Fixture DXF CLI still 6/6. Live DWG extract works; title-block terms like 图号 are not in builtin YAML so they need an engine. Do not invent 批量导入. Do not touch docs/icons/.
- 2026-08-28: Packing fix. Windows version from `APP_VERSION` (`installer/pack_version.py` → ISCC `/DMyAppVersion=`). Tag must match `v{APP_VERSION}`; `workflow_dispatch` uploads an artifact and does not overwrite a live release. Inno Start Menu name is 图译; exe still `Dwglot.exe`. Dropped `[UninstallDelete] {app}`. `UsePreviousAppDir=yes`. macOS: `Dwglot_macos.spec` + `installer/build_macos.py` build `Dwglot.app` (Dock 图译, id `com.erict16.dwglot`), no ODA DMG, Noto + same hiddenimports as Windows. CI: `.github/workflows/macos-release.yml` on macos-latest (Apple Silicon). Honsen 1.8.8 spec/iss moved to `quarantine/`. Landing `DL.mac` still empty until a DMG exists. Do not rename the repo. Do not invent 批量导入. Do not touch docs/icons/.
- 2026-08-28: v0.1.2 Windows pack. One Analysis onedir + COLLECT: `Dwglot.exe` and `dwglot-cli.exe` share `_internal`. v0.1.1 Setup ~138 MB was two one-file Pythons (numpy/matplotlib/Noto twice). Dropped extra matplotlib data collect; strip mpl-data sample_data/images and ezdxf Cython sources. PDF and Noto stay. No ODA. `APP_VERSION` 0.1.2; Inno → `Dwglot_v0.1.2_Setup.exe`; tag `v0.1.2`. Landing `DL.win` = v0.1.2 Setup; mac empty. `python -m dwglot translate` fixture DXF + PDF `%PDF-1.4` ok. Suite 168 green. Do not invent 批量导入. Do not touch docs/icons/.
- 2026-08-28: v0.1.1 Windows pack. `APP_VERSION` 0.1.1; Inno → `Dwglot_v0.1.1_Setup.exe`; Actions release name 图译 Dwglot v0.1.1; tag `v0.1.1`. `dwglot-cli.exe` still next to `Dwglot.exe` (not renamed). No ODA, no signing, no Sparkle. Landing `DL.win` = GitHub Release `v0.1.1` Setup.exe; mac empty. `.github/workflows/windows-release.yml` kept in sync with `installer/windows-release.yml` (CLI check included). Do not invent 批量导入. Do not touch docs/icons/.
- 2026-08-28: Agent CLI `python -m dwglot translate INPUT [-o OUTPUT]`. Same 常规 写回 pipeline. Windows pack adds console `dwglot-cli.exe` next to `Dwglot.exe` (NTFS cannot host both `dwglot.exe` and `Dwglot.exe`). No second translator, 批量导入 still 501, no ODA in payload. Suite 166 green.
- 2026-08-28: v0.1.0 Windows pack is 图译/Dwglot: `Dwglot.spec` → `dist/Dwglot.exe`, Inno `installer/Dwglot_Setup.iss` → `Dwglot_v0.1.0_Setup.exe`. No ODA, no signing. Workflow source is `installer/windows-release.yml` (this OAuth token cannot push `.github/workflows/`). Landing `DL.win` = GitHub Release `v0.1.0` Setup.exe; mac empty. Live HTTP 工作位置表 off/on + floor_plan.pdf ok. Suite 156 green. Do not touch docs/icons/.
- 2026-08-28: 常规参数 **同时翻译文件名** (default off). Off: `en_工作位置表及接线原理图10191W-CV2_…`. On: CJK in the stem via glossary then MT; codes like `10191W-CV2` stay; no hit → CJK stays (`天花10191W-CV2` → `en_ceiling10191W-CV2_…`). GET `/api/default-output-name?translate_filename=` and 写回/batch start honor it. Strip `/ \ : * ? " < > |`. Do not edit landing/, README*, docs/icons/.
- 2026-08-28: Eric picked the navy+steel fox as the temporary official icon. Copied `/workspace/icon-rca/recolors/01-navy-steel.png` → `docs/icons/app.png` (no image_gen, no `generate_icons.py`). Same 角尺狐 silhouette: navy body, steel set-square, white ground. `01.png`–`08.png` kept as rejected candidates. Live mark: `ico.ico` / `icon.ico` generated from that PNG (Windows spec + Tk `resource_path("ico.ico")`); README + README_CN `docs/screenshots/mark.svg` and landing `shots/mark.svg` (favicon + header) are SVG wrappers of the same PNG. No backend/API/test/feature edits. Old Honsen ico gone.
- 2026-08-27: Linux DWG 链路: ezdxf treated ODA 27.1 Qt `XDG_RUNTIME_DIR` stderr as failure so convert never finished. `convert_with_odafc` now runs ODA CLI + xvfb and succeeds when the file exists. Local tool only: `~/.local/bin/ODAFileConverter` (AppImage extract under `~/.local/oda`, not in the repo, not bundled). Live HTTP on `工作位置表及接线原理图10191W-CV2.dwg`: open → extract 76 CJK → glossary 15 → writeback DWG with English (`Number of tap positions`, `Change-over selector`). Builtin YAML is architectural so 0 hits without domain terms; no MT key. 批量导入 still 501. `python -m unittest discover -s tests` stays green. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Icon candidates in `docs/icons/` overwritten from Grok imagine (`image_gen`), six separate calls, jpg→png copy, no redraw. A 折纸鹤, B 墨鱼, C 角尺狐. Pure white `#FFFFFF`. Last batch threw imagine out and shipped `generate_icons.py` polygons; this round keeps the imagine files (`a1`–`c2`). No winner. Landing / README / app untouched.
- 2026-08-27: `GET /api/language-assets` is 200. Returns `builtin_terms` (YAML, non-empty), `terms`, `project`, `memory`, `usage`. Launch refresh counts builtin+terms. Missing/junk project stays 200 with 术语表不存在, no Traceback. POST project/import already covered. `python -m unittest discover -s tests` 154 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `GET /api/odafc-status` is 200. No ODA → `installed` false, empty path, `message` has 未检测到 ODA. Installed → path and `source` in env/bundled/system. No Traceback. Launch refresh uses it. `python -m unittest discover -s tests` 153 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `GET /api/default-output-name` is 200. `zh_to_en` + base `floor_plan` → `en_floor_plan_…`; empty base → `translated_cad_…`; `zh-Hans_to_ja` + `平面图` → `ja_平面图_…`. No Traceback. Helper was already covered; this locks the HTTP 写回 uses. `python -m unittest discover -s tests` 152 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `GET /api/logs/stream` is 200 `text/event-stream`. A log line is `data: {"type":"log",...}`; `set_status` is a status event; idle ticks are `: keepalive`. No Traceback. TestClient.stream hangs on the infinite generator; the lock hits `stream_logs()` directly. `python -m unittest discover -s tests` 151 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/batch/{id}/retry` of a failed task is 200, `queued` / `等待重翻`, progress 0, output_file empty. `python -m unittest discover -s tests` 150 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/batch/{id}/remove` of a running task was 200 with the task still in the snapshot. Now 409 `请先停止队列`. After stop, remove is 200 and `tasks` []. `python -m unittest discover -s tests` 149 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/batch/pause` JSON `{"paused": false}` was ignored (query default still paused). Query `paused=false` already worked. JSON body now wins when `paused` is present. `python -m unittest discover -s tests` 148 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/batch/clear` while the queue is running is 409 `请先停止队列`. After stop, clear is 200 and `tasks` []. `python -m unittest discover -s tests` 147 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/batch/stop` during a running job is 200, task `cancelled` / `已停止` (stop() writes that before the worker). InterruptedError without cancel flag is `failed` / `翻译已取消`, retries 0. `python -m unittest discover -s tests` 146 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `InterruptedError("translation cancelled/stopped")` was English; if cancel flag was unset, `_calm_error` → 翻译失败 and it was retryable. Now 翻译已取消 / 翻译已停止, not retried. Flag still 应用关闭，可重新开始. `python -m unittest discover -s tests` 145 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: batch `_run_batch` wrapped English `ValueError` (`layout bbox is invalid`) as `_fatal_batch_error(str(exc))`. Now `_chinese_detail` → queue `翻译失败`. `无法读取DXF文件` still that line. `python -m unittest discover -s tests` 145 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: extract/export-pdf FileNotFoundError 404 used `str(exc)` (`No such file or directory`). Now 图纸不存在. Print maps that 404 to 400 图纸不存在. 写回 already hardcoded. `python -m unittest discover -s tests` 144 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: DeepL/Azure/Ollama/custom `TranslationProviderError` appended `{exc}` (auth failed, network unavailable, invalid, model not found). Now Chinese only: 初始化失败 / 翻译失败 / 请求失败. Quota 403001 still 免费额度已用尽. `python -m unittest discover -s tests` 143 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: batch `_calm_error` used English `str(exc)` (`No space left on device`, `boom`+Traceback) as the queue message. Now keeps Chinese (`无法读取DXF文件`, ODA line) and falls back to `翻译失败`. `python -m unittest discover -s tests` 142 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: extract / translate / writeback / export-pdf passed English `ValueError`/`RuntimeError` (`layout bbox is invalid`) as 400 body. `_chinese_detail` keeps Chinese (`无法读取`, `文件保存失败`) and falls back (提取失败 / 翻译失败 / 写回失败 / 导出 PDF 失败). `python -m unittest discover -s tests` 142 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/drawings/export-pdf` (and print) unknown exceptions were 500 `导出 PDF 失败: unsupported operand type`. Now 400 `导出 PDF 失败` with no English body. OSError save still 400 `文件保存失败`. `python -m unittest discover -s tests` 141 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/drawings/writeback` unknown exceptions were 500 `写回失败: No space left on device`. Now 400 `写回失败` with no English body. Save fail still 400 `文件保存失败`. `python -m unittest discover -s tests` 140 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/drawings/translate` unknown exceptions were 500 `翻译失败: No space left on device`. Now 400 `翻译失败` with no English body. Provider errors still 400 with their Chinese text. `python -m unittest discover -s tests` 139 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/drawings/extract` unknown exceptions were 500 `提取失败: No space left on device`. Now 400 `提取失败` with no English body. Unreadable DXF still 400 `无法读取`. `python -m unittest discover -s tests` 138 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: export-pdf/print save fail was 500 `导出 PDF 失败: No space left on device`. OSError in `_render_pdf` is now 400 `文件保存失败`. `python -m unittest discover -s tests` 137 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 常规 `writeback_rows` save fail was 500 `写回失败: No space left on device`. Now 400 `文件保存失败`. Batch `translate_cad_file` already that line. `python -m unittest discover -s tests` 136 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `translate_cad_file` save fail logged `文件保存失败: No space left on device` and re-raised the English OSError (batch would retry). Now log/raise `文件保存失败` (ValueError). `python -m unittest discover -s tests` 135 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/translate` (`start_translation`) logged `ERROR: traceback.format_exc()` on fail. Now Chinese: English exceptions → 翻译失败; 无法读取DXF文件 stays. UI still uses `/api/drawings/translate`. `python -m unittest discover -s tests` 134 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: batch `write_back_translation` logged `traceback.format_exc()` (English File/line). Now `写回失败` only; batch wrapper is `写回 CAD 实体失败` without `{e}`. 常规 写回 never dumped a traceback. `python -m unittest discover -s tests` 133 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 写回 / export-pdf / print with `output_dir` as a file is 400 `输出目录无法创建` (no FileExistsError). Batch start already locked. `python -m unittest discover -s tests` 133 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: stub DWG print without ODA is 400 `未检测到 ODA`. Open/extract/pdf/writeback of DWG without ODA already locked. `python -m unittest discover -s tests` 132 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/batch/start` with `output_dir` that is a file was an uncaught `FileExistsError` (500). Now 400 `输出目录无法创建`. Same helper on 写回 and export-pdf. `python -m unittest discover -s tests` 131 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Batch of junk DXF already failed 无法读取DXF文件, but logs were `读取文件失败: File '…' is not a DXF file.` Now logs 无法读取DXF文件, no English ezdxf OSError. `python -m unittest discover -s tests` 130 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `lp` exit 1 with English stderr (`No such file or directory`) or empty stderr was leaking English / claiming 系统没有打印命令. Now 打印失败，PDF 已留下. Missing `lp` still 系统没有打印命令. `python -m unittest discover -s tests` 130 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: export-pdf/print `style=译原对照` + `layout=A1` is 200, 1 CJK page, different bytes than 纯译文 A1 and 原译对照 A1. Missing `lp` leaves the PDF. Batch has no PDF layout. `python -m unittest discover -s tests` 129 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 译原对照 write-back of floor_plan A1: original handles become `grounding` / `floor plan`, stacked `接地` / `平面布置图` stay on A1 (4 TEXT). Those stay off Model. `python -m unittest discover -s tests` 128 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 对照 write-back of floor_plan A1 paperspace TEXT stacks `接地`/`grounding` on A1 (4 TEXT). `grounding`/`接地` stay off Model. Model still has `天花图` + `reflected ceiling plan`. Batch has no PDF layout. `python -m unittest discover -s tests` 127 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: export-pdf/print `style=原译对照` + `layout=A1` is 200, 1 CJK page, different bytes than 纯译文 A1. Missing `lp` leaves the PDF. Batch has no PDF layout. `python -m unittest discover -s tests` 126 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Empty-entity DXF extract is 200, `count`/`unique` 0, `items` []. Stop adding empty-file tests. Batch has no PDF layout. `python -m unittest discover -s tests` 125 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Junk/zero-byte DXF extract was already 400 `无法读取DXF文件`. export-pdf, print, and 写回 were 500 with the English ezdxf `OSError`. Same `_read_dxf` now raises ValueError, so those routes are 400 `无法读取` too. Batch already failed calmly. `python -m unittest discover -s tests` 124 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: DXF with no entities (empty Model + empty Layout1) still export-pdf/print: `_layout_pages` falls back to modelspace, 200, 1-page `%PDF-`, no traceback. Missing `lp` leaves the PDF. Batch has no PDF layout. `python -m unittest discover -s tests` 123 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/drawings/export-pdf` `print_after` is 200. Missing `lp` leaves the CJK PDF (`print.ok` false, 系统没有打印命令). Mocked `lp` exit 0 is `print.ok` true. UI still uses `/print`. Batch has no PDF layout. `python -m unittest discover -s tests` 121 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: floor_plan `layout=Model` is named modelspace (not paperspace). export-pdf and print are 200, 1 CJK page, fewer pages than all layouts. Missing `lp` still leaves the PDF. Batch has no PDF layout. `python -m unittest discover -s tests` 120 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: floor_plan `Layout1` is empty paperspace. export-pdf and print with `layout=Layout1` is 200, 1-page `%PDF-` (no CJK ink required), no traceback. Missing `lp` still leaves the PDF. Batch has no PDF layout. `python -m unittest discover -s tests` 118 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: floor_plan layouts Model / Layout1 / A1. export-pdf and print with `layout=A1` is 200, 1 page, CJK PDF (fewer pages than all layouts). Batch has no PDF layout. `python -m unittest discover -s tests` 116 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Unknown PDF layout is 400 `没有这个布局` on `/api/drawings/export-pdf` and `/api/drawings/print`. Batch has no PDF layout. `python -m unittest discover -s tests` 114 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Locked + off layers: extract skip TITLE `天花图`, batch `include_locked/include_off` False leaves it and still writes `剪力墙` → shear wall. Same as 常规. `python -m unittest discover -s tests` 112 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 常规 翻译 posts `visibleRows` and `/api/drawings/translate` applies skip_numbers/dupes/nonsource (default on). Hidden rows stay in state without a target. Proof: 天花图 + duplicate + 1234 + HELLO → only 天花图 gets `reflected ceiling plan`. `python -m unittest discover -s tests` 110 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 常规 写回 posts `visibleRows`, not the full extract `rows`. Toggling 过滤 after extract no longer writes hidden rows that already have a target. skip_dupes write-back of floor_plan leaves paperspace `平面布置图`, still writes unique `接地` and model `floor plan`. `python -m unittest discover -s tests` 108 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 纯数字 / 重复 / 非源语言 skip is in `item_passes_text_filters` (drawings extract + batch `extract_text_entities`). Defaults on for `/api/drawings/extract` and `/api/batch/start`. 开始导出 sends `skip_numbers/dupes/nonsource` from the checkboxes. Duplicate paperspace copies stay source when 重复 is on. `python -m unittest discover -s tests` 107 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/batch/start` on an all-succeeded queue re-runs those rows (same task id, new output). Skip already-running (200 `没有待处理的图纸`). Empty queue still 400 `请选择 CAD 文件`. 常规 DWG-without-ODA still 400. `python -m unittest discover -s tests` 105 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: `/api/batch/add` does not enqueue a second row for the same path (abspath/normcase). 开始导出 add+start reuses the existing task id. New paths still append. 常规 DWG-without-ODA still 400. `python -m unittest discover -s tests` 103 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Batch honors 模型 / 图纸空间 / 冻结图层 (and 锁定 / 关闭), same defaults as 常规. floor_plan: 图纸空间 off leaves `接地`; 模型 off leaves `天花图`; frozen TITLE leaves `天花图`, still writes `剪力墙`. 开始导出 sends params.model/paper/frozen/locked/off. 常规 DWG-without-ODA still 400. `python -m unittest discover -s tests` 102 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Batch honors 块属性 (`include_attribs`, default on, same as 常规). Off: floor_plan INSERT ATTRIB/ATTDEF stay `配电箱`; TEXT still translates. 开始导出 sends `include_attribs: params.attribs`. 常规 DWG-without-ODA still 400. `python -m unittest discover -s tests` 99 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Batch 对照 no longer double-stamps modelspace. `include_blocks` was re-scanning `*Model_Space` / `*Paper_Space*` (names differ from layout `Model`), so 原译对照 wrote two `reflected ceiling plan` TEXT at the same point. Skip those layout blocks. 开始导出 no longer maps 块属性 → `translate_blocks`. 常规 DWG-without-ODA still 400. `python -m unittest discover -s tests` 98 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Batch now honors 标注、表格 (`enable_v02`, default on, same as 常规). dims_tables.dxf batch writes DIMENSION `安装高度` and ACAD_TABLE 302 (`墙体拆除图` / `材料表`) to English, not only *T preview TEXT. 对照 write-back stamps table cells as `源 / 译` (single-line, like ATTRIB/DIMENSION). 常规 DWG-without-ODA still 400. 批量导入 still 501. `python -m unittest discover -s tests` 96 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Live DWG without ODA no longer 400s the whole `/api/batch/start`. Add/drop queue the DWG; start marks it `failed` (`未检测到 ODA`) and still runs DXF. 常规 `/api/drawings/open` stays 400. Proof: floor_plan.dxf + a DWG from `/workspace/dwglot-drawings/`. 批量导入 still 501. Do not bundle ODA. `python -m unittest discover -s tests` 94 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Queue load/start marks missing CAD paths `failed` (`图纸不存在`) and does not 400 the whole `/api/batch/start`. Mixed live + stale entries: live still runs. `python -m unittest discover -s tests` 93 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: ATTRIB/ATTDEF 对照 stays one line (`源 / 译`); AutoCAD attributes cannot hold `\\P`. Unreadable DXF is 400 `无法读取DXF文件` on extract and a non-retry batch fail (was retried as generic Exception). `python -m unittest discover -s tests` 92 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 批量导出 honors 版式. `translate_cad_file(..., style=)` reuses `apply_pdf_style`. 纯译文 unchanged; 原译对照 / 译原对照 stack TEXT and MTEXT `\\P` with `\\C1;`. floor_plan.dxf batch 对照 re-reads 天花图 and reflected ceiling plan. `python -m unittest discover -s tests` 90 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 对照 write-back. 写回 纯译文 is translation only. 原译对照 / 译原对照 reuse `apply_pdf_style`: stacked TEXT, MTEXT/MULTILEADER `\\P` with `\\C1;` kept. Re-read DXF has both 天花图 and reflected ceiling plan. PDF of the write-back is the file as written. `python -m unittest discover -s tests` 88 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Batch `translate_cad_file` kept MTEXT/MULTILEADER codes (`{\\C1;天花}` / `隔墙定位图`). Same run mapper as 常规. floor_plan.dxf batch output still has `\\C1;`. 对照 write-back DXF still 纯译文 (PDF stamp only). `python -m unittest discover -s tests` 87 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 批量导出 glossary-only. Empty DeepL/Azure/OpenAI/Ollama is allowed; bundled zh→en hits write a prefixed DXF. Unmatched rows stay source (`未译（无引擎）`); if nothing hits, the task fails calmly (not success-with-source). floor_plan.dxf 天花图/平面布置图 → English, no network. DWG without ODA still 400; 批量导入 still 501. `python -m unittest discover -s tests` 86 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 版式 hits PDF/打印 after 写回. 纯译文 draws the write-back as-is. 原译对照 / 译原对照 stamp a second TEXT line (or `\\P` on MTEXT/MULTILEADER) from table source+target; PDF bytes differ per 版式. Before 写回 still `原图，还未写回译文` (no overlay). `python -m unittest discover -s tests` 85 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: After 写回, 导出 PDF / 打印 uses the written DXF (`writtenPath` / `cad_path`). Before 写回, PDF of the open source is honest: `原图，还未写回译文`. Proof: 天花图 → reflected ceiling plan write-back, then PDF of that file; `cad_path` is the write-back, PDF bytes differ from the source PDF. `python -m unittest discover -s tests` 84 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: MULTILEADER on the 常规 loop. `{\C1;天花图}` was extracted then write-back stripped the codes (unlike MTEXT). Translate now maps MTEXT runs for MULTILEADER; write-back keeps `\\C1;`. ATTDEF in the TITLE block is asserted. Paperspace 接地 still round-trips. `python -m unittest discover -s tests` 84 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Config / 方向 / 引擎. Corrupt `~/.dwglot_config.json` (bad JSON, array, null, unknown provider, null keys) is quarantined and GET `/api/config` returns defaults — no launch crash. Empty-table 中→英 / 英→中 translate is 200. Queue `[]`/`null` also recovers. Empty 云/本地/自定义 keys unchanged. `python -m unittest discover -s tests` 83 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 过滤 / 字数 / 图层. Filter-all-out shows `过滤后没有可显示的文字。` (table may be empty). Digits-only rows still extract; empty/None/int layer is coerced to text and does not throw. Footer counts use `asCount`. No new filter product. `python -m unittest discover -s tests` 80 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 打印 is system `lp`/`lpr` of the ezdxf PDF, not AutoCAD plot. Missing `lp`/`lpr` returns a calm `系统没有打印命令，已留下 PDF` (no hang, timeout 5s). Print with no drawing / missing path is HTTP 400. CJK PDF ink still asserted on the print path. `python -m unittest discover -s tests` 77 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Custom OpenAI-compat URL that is set but dead/hangs is probed in 2s (`GET {base}/models`). Batch start 400 `无法连接自定义接口`; 常规 glossary-only still matches, unmatched `needs_engine`, no 60s hang, no success, no traceback. Translate timeout capped at 15s. Missing extract/export-pdf path is 404 `图纸不存在`. Dead-port unit test, no live paid APIs. `python -m unittest discover -s tests` 73 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Engine + glossary load. Empty DeepL/Azure key, down Ollama (2s `/api/tags` probe), and empty custom URL fail calmly: no DeepSeek default, no 120s hang, batch start 400, do not mark success. Glossary-only 常规 translate still matches YAML terms with no key; unmatched rows are `needs_engine`. Missing/empty/bad-encoding glossary → 400 (`术语表不存在` / `术语表是空的` / `请另存为 UTF-8`), no traceback. Empty CSV import 400. Mock/empty-key tests only; no live paid APIs. `python -m unittest discover -s tests` 72 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 批量导入 is an honest stub: UI says 还没做; `POST /api/batch/import` is HTTP 501. Open with no files, write-back with no rows, missing DXF, and missing glossary package return 400/404 without errno/traceback. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: 批量导出: empty add/start, missing path, and DWG without ODA return HTTP 400 with the short line, no traceback. Queue does not retry those. 批量导入 is still the stub (“先走写回”). Live DXF batch still needs a DeepL/Azure/Ollama key. `python -m unittest discover -s tests` 61 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: ACAD_TABLE write-back now patches the anonymous `*T` preview TEXT/MTEXT to match the 302 cell (same source string). That is the Honsen regen strategy; we still cannot run AutoCAD itself here. `test_batch_queue.py` and `test_language_assets.py` are unittest.TestCase, so `unittest discover` counts them. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Bug hunt. GitHub 403/malformed JSON on 检查更新 returns HTTP 200 with 暂不可用, not a crash; UI catch uses the same line. 参数 sheet 完成 re-extracts so 标注、表格 (on by default, not v0.2) actually applies. Unittest discover now includes updates 403 + license path. Committed `tests/fixtures/floor_plan.dxf` and `dims_tables.dxf` extract → glossary → write-back → CJK PDF (raster ink, not tofu). 7 real DWGs still 400 without ODA. `python -m unittest discover -s tests` 53 ok. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: DIMENSION override + ACAD_TABLE group-code 302 extract → glossary → write-back shipped in v0.1. UI 标注、表格 (`enable_v02`, on by default). `<>` stays untranslated. Fixture `tests/fixtures/dims_tables.dxf`: 安装高度 / 墙体拆除图 / 材料表 / 天花图 round-trip to English, re-read, PDF `%PDF-`. AutoCAD table regen after 302 write is still unproven. Default extract without the flag still skips DIMENSION/ACAD_TABLE. DXF loop, CJK PDF, 7 real DWGs 400 without ODA stay green. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Bundled `fonts/NotoSansSC-Regular.otf` (Noto Sans CJK SC subset, OFL) and register it with ezdxf drawing before PDF. 平面布置图 is real glyph outlines (278 verts); raster PDF has ~5e5 dark pixels, not tofu. pdftotext stays empty because ezdxf draws paths. `odafc_candidate_paths()` resolves posix paths so `/tmp` vs `/private/tmp` matches; Windows drive paths stay un-resolved on Linux. Frozen-macOS ODA test compares resolved adjacent + Helpers paths. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Icon candidates in `docs/icons/` overwritten. Rejected subjects now also include 图卷貘 / 对译河马 / 层纹獾 (plus 图纸鸭 / 读图鸮 / 图板龟). New directions: A 折纸鹤, B 墨鱼, C 角尺狐. Pure white `#FFFFFF`, flat silkscreen plates, `#007AFF` not used. Six 1536 PNG (`a1`–`c2`). Image-gen came back as cardboard photos / cute stickers and was thrown out; shipped marks are constructed ink plates. No winner. Landing / README / app untouched.
- 2026-08-27: Real Huaming DWGs in `/workspace/dwglot-drawings/` (7 files, AC1021/AC1027). No ODA on this box; cannot convert without bundling ODA or linking LibreDWG (forbidden). All 7 fail clean HTTP 400 / RuntimeError with the short ODA message on extract, write-back, PDF, open, batch drop — no traceback. Write-back used to dump the long ODA essay; now the same short line as extract. Fixture DXF still: extract 8 → glossary 8 → write-back 8 → PDF 2 pages. Regression `RealDwgWithoutOdaTests` skips if the folder is missing.
- 2026-08-27: Overnight harden of the regular loop. Bugs fixed: PDF skipped modelspace (drew empty Layout1); SHX rewrite clobbered OpenSans TTFs because empty bigfont counted as SHX; 平面布置图/隔墙定位图 missing from zh↔en glossary; duplicate paperspace strings never wrote back; DWG upload with no ODA failed only at extract. Fixture `tests/fixtures/floor_plan.dxf` (walls + TEXT/MTEXT/attribs + A1). Proof: extract 8 → glossary 8 → write-back 8 including paperspace floor plan/grounding → PDF `%PDF-1.4` 2 pages. No ODA here, DWG rejected with the ODA message. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: v0.1 regular-processing loop works. Open DWG/DXF (native picker or upload), extract TEXT/MTEXT/ATTRIB/ATTDEF/MULTILEADER (DIMENSION/ACAD_TABLE gated), glossary-first 翻译 into the table (云 DeepL/Azure, 本地 Ollama, 自定义 OpenAI-compat), 写回 to a new file, 检查更新 via GitHub Releases. 导出 PDF uses ezdxf drawing + matplotlib (DWG → ODA → DXF → PDF; not AutoCAD plot). 打印 = system print of that PDF (`lpr`/`lp`/`os.startfile`). Config/queue/glossary paths are `~/.dwglot_*` with a one-time migrate from Honsen names. Live DXF: 天花图/剪力墙/接地 → glossary English, MTEXT kept `{\C1;…}`, write-back DXF, PDF `%PDF-`, updates check did not crash. Do not edit landing/, README*, docs/icons/.
- 2026-08-27: Icon candidates in `docs/icons/` overwritten. Eric killed the cream/denim 3D batch (图纸鸭 / 读图鸮 / 图板龟). New subjects on pure white `#FFFFFF`, two inks `#007AFF` + `#1A1A1A`, flat silkscreen: A 图卷貘, B 对译河马, C 层纹獾. Six independent 1024 PNG (`a1`–`c2`). No winner. Landing / README / app untouched.
- 2026-08-27: Landing download is for ordinary people. Header/hero 下载 go to `#download` (macOS / Windows buttons). `const DL = { mac: "", win: "" }` at the top of `landing/index.html`; empty click says 安装包还没放上来, does not send them to GitHub. GitHub icon + Releases only in the footer. OS highlight if detectable. Hero gap kept. Live on gh-pages.
- 2026-08-27: Landing `landing/index.html` follows Tailwind Plus **Salient** (hero + screenshot, feature tabs, download CTA, FAQ). Vercel rewrites `/` to it. Shots are the running app, not HTML sketches.
- 2026-08-27: Repo is **public** MIT `erict16/dwglot`. Description + topics (dwg, dxf, cad, translator, converter, opendwg, ezdxf, macos, windows, glossary). README.md + README_CN.md follow tw93/MiaoYan layout; 安装 spells unsigned Gatekeeper (右键打开 / Open Anyway) and SmartScreen (更多信息 → 仍要运行). Real-app shots in `docs/screenshots/`. Do not sign v0.1. Do not UPX.
- 2026-08-27: v0.1 first cut. Mac sketches locked (01 = main). PLAN: auto-update is v0.1 (GitHub Releases + Sparkle/WinSparkle + Python fallback). Licence/QR/keygen moved to `quarantine/` and dropped from `backend/api.py`. Frontend rebuilt to Mac sketch 01 (`frontend/src/App.jsx` + `App.css`); 批量导出 and 参数 sheet exist. 检查更新 → `/api/updates/check` → `erict16/dwglot` Releases. Window title 图译 Dwglot, default zh→en. Honsen stack kept. Dist built; `sketches/app-01.png` is the running UI.
- 2026-08-27: Sketches switched to **Mac design language**. Eric: 不要传统 Windows 软件；主要按 mac 做. Traffic lights left, unified toolbar, sidebar, sheet/popover for 参数, no File/Edit menubar, no right-side caption buttons. Same 轻语-genre IA (常规/批量, 原文译文表, 云/本地/自定义, 版式, 过滤, 字数, ODA, 术语表 393). Five 1280×800 frames: 01 常规 light, 02 denser + popover, 03 批量导出 + inspector, 04 参数 sheet, 05 macOS dark. longshot 1280 → 01.png–05.png. Sketch HTML only.
- 2026-08-27: Sketch redesign to 轻语-genre **desktop window**, not Converter Pro card. Eric: 重新设计、不要照抄、各个 feature 都应该有、照着轻语 app；浅色为主深色为辅；最后是桌面端. Five full 1280×800 frames in `sketches/01.html`–`05.html` (title bar + client): 01 常规处理 light, 02 denser + MT float, 03 批量导出, 04 参数设置 dialog, 05 dark of 01. IA: tabs 常规/批量导出/批量导入, toolbar 打开图纸/加载术语表/参数设置/翻译/写回图纸, left file list, 原文/译文 table, 中→英, 云/本地/自定义, 块属性/标注/图层/模型·图纸空间, 纯译文/原译对照/译原对照, 过滤, 字数, ODA, 术语表 393. longshot 1280 → 01.png–05.png. No app implement, no 轻语 unpack, no ODA bundle, no licence UI.
- 2026-08-27: Sketch-only, left/right Converter Pro shell. Eric: 「可以改成左右排列吗」plus strip bloated chrome. Killed hero title, 3 stat cards, 术语表 nav. Kept thin brand 图译 Dwglot. Left dropzone (~220px); right thin queue + one 翻译 + one status line; small 云/本地/自定义 under the right column. Tokens stay in `sketches/_frame.css` (Segoe UI, 1040, dark/light). Overwrote `sketches/01.html` empty dark, `02.html` queue dark, `03.html` same as 02 light; `~/.local/bin/longshot --width 1280` → `01.png` `02.png` `03.png`. No app implementation, no clone of dwg-converter-pro, no 原文/译文, no GenerateImage. Layout lock recorded in `SKETCH-LOCK.md`.
- 2026-08-17: Root-caused the reported wholly untranslated Layout1 using the supplied DWG converted to a temporary DXF. ODA represents legacy Chinese/GBK text there as `\\M+5C6BD`-style strings (for example `\\M+5C6BD\\M+5C3E6` = “平面”), which the validity filter had skipped. Added decoding before cleaning/validation so the actual Layout1 now yields 98 translation items, including “平面布置图” and “隔墙定位图”. The decoder also restores GBK-escaped accented Latin. Added regression coverage; translation-mode tests pass (18) and platform tests pass (10). A new macOS package is being rebuilt from this root-cause fix.
- 2026-08-17: Fixed a further `Layout1` table path. Native `ACAD_TABLE` objects retain authoritative cell text in `AcDbTable` group-code 302 records; updating only their anonymous `*T` rendered block can be undone when AutoCAD regenerates the table. The translator now extracts and writes these source cells as well. Added a write-back regression; translation-mode tests (17), platform tests (10), frontend build, and a rebuilt arm64 app/DMG pass.
- 2026-08-17: Expanded CAD text coverage after reports that drawing titles, legends and sheet-index labels were left untranslated. The desktop UI now enables block-definition translation by default (recommended). Even when that option is disabled, extraction still scans visible anonymous `*T` table and `*D` dimension blocks; `DIMENSION.dxf.text` overrides are translated while the CAD measurement placeholder `<>` is preserved. Added regressions for anonymous table-block and dimension override collection. Translation-mode tests (16), platform tests (10), frontend build, DMG checksum, and app signature verification pass. Rebuilt `dist/Honsen CAD Translator.app` and `dist/Honsen_CAD_Translator_v1.8.8_macOS_arm64.dmg`; the local changes are intentionally not committed/pushed yet.
- 2026-08-17: Fixed the macOS ODA dialog “Output version `R2010` is invalid.” The DWG working-copy conversion had passed ezdxf’s DXF label `R2010` directly to ODA; ODA accepts only `ACAD*` identifiers, so it now passes `ACAD2010`. The human log remains “AutoCAD 2010”. Added a regression asserting the working-copy version belongs to `ODA_OUTPUT_VERSIONS`; platform tests (10) and translation tests (14) pass. The macOS app and `dist/Honsen_CAD_Translator_v1.8.8_macOS_arm64.dmg` were rebuilt and verified. The normal Homebrew Python 3.12 environment had a pyexpat/Expat linker defect, so this build used an isolated temporary arm64 Python 3.9 environment; `desktop/launcher.py` now defers annotations for Python 3.9 compatibility. The app/Disk-image signature is local ad-hoc only.
- 2026-08-15: Corrected the initially published typo version `1.18.8` to the intended `1.8.8` across source, Windows/macOS packaging, metadata, documentation, and release labels. `installer/build_macos.py --dmg` emitted `dist/Honsen_CAD_Translator_v1.8.8_macOS_arm64.dmg`; `hdiutil verify` passed and the arm64 application is valid under `codesign --verify --deep --strict`. Automated platform tests (9) and frontend production build passed. Commit `ce68c05` and tag `v1.8.8` were pushed to GitHub and Gitee; the accidental `v1.18.8` tag was deleted from both. Gitee intentionally uses `git@gitee.com:etianwang/CAD-translator.git` with the configured SSH key. This local ad-hoc signed build is not notarized; the mandatory credentialed desktop E2E release gate remains outstanding.
- 2026-08-15: Briefly experimented with making the macOS “果冻” theme transparent enough to reveal background windows, then reverted it at the user's request because the result was visually worse. The current “果冻” theme is restored to its prior opaque animated background and balanced glass/input opacity; only the Liquid Glass-inspired in-app layering remains.
- 2026-08-15: Redesigned the first, system-accent palette as a macOS Liquid Glass-inspired “果冻” theme. It uses the macOS accent colour as ambient light rather than a fixed blue; adds a restrained colourful background bloom, regular blurred materials for long-lived content panels, and clearer/translucent specular glass for the top bar and primary controls. This follows Apple’s guidance to treat Liquid Glass as a distinct navigation/control layer rather than applying it indiscriminately to all content. Purple and forest remain unchanged. Frontend production build and diff check passed; macOS package rebuilt afterwards.
- 2026-08-15: Replaced the fixed blue first UI palette with a system-accent palette. On macOS, `/api/system-theme` reads `NSColor.controlAccentColor` in sRGB through AppKit at startup and feeds that RGB value into the animated background, controls, glow, and theme indicator; the current local system value read as `[0.0, 0.4784, 1.0]`. If the native API is unavailable, a neutral Graphite-like colour is used rather than a hard-coded blue. Purple and forest palettes remain explicit manual alternatives. Added a mocked macOS accent-colour regression; 9 platform tests, 14 translation tests, frontend build, and diff check passed.
- 2026-08-15: Added a best-effort macOS ODA background launch through `open -g -j -W -n -a <ODA app> --args …`, while retaining direct ezdxf fallback for non-bundle executables. The complete updated path, including Unicode ASCII staging, was live-tested against the reported `Mât` DWG and produced an AutoCAD 2010 DXF; regression tests passed. Subsequent user testing showed that ODA 27.1 can override the macOS background/hide request and foreground its own Qt window. Official ezdxf documentation confirms there is no known truly headless macOS ODA mode, and the ODA 27.1 bundle has no `--no-gui`/headless option. Do not represent this packaged ODA approach as a `nowindow` guarantee; meeting that requirement needs a separately licensed non-GUI conversion engine/SDK.
- 2026-08-15: Fixed two macOS desktop defects reported from the packaged app. The animated Iridescence background now has a CSS theme-colour first-frame fallback and tracks WebKit container resize, so the default theme is visible on first open rather than only after a theme switch. macOS ODA 27.1 was shown to fail when ezdxf passes a decomposed-Unicode filename (such as `Mât`) as the input filter, producing the ODA “There is no matched files in input folder” dialog. All macOS conversions now stage the source as an ASCII `input.dwg`/`input.dxf` copy in a private temporary directory before calling ODA; a direct test on the reported DWG produced a DXF successfully. The later hidden-LaunchServices update supersedes the original direct ODA launch. Added Unicode-staging regression coverage; 8 platform tests, 14 translation tests, frontend production build, and diff check passed.
- 2026-08-15: Confirmed and documented the macOS default output location as `~/Documents/Honsen CAD output` (shown by Finder as “文稿/Honsen CAD output”). The directory is created on first use; Windows keeps its existing `Documents/Honsen CAD output` behaviour. Added a macOS regression check for the default path.
- 2026-08-15: Cloned the GitHub repository `https://github.com/etianwang/CAD_translator.git` into the macOS workspace. Baseline is clean `main` at commit `88dced8` (`docs: record refactor push`); no product or build files were changed during checkout.
- 2026-08-15: Added macOS compatibility without changing the existing Windows PyInstaller spec or Inno Setup workflow. The desktop launcher keeps EdgeChromium on Windows and uses pywebview's native Cocoa/WebKit default elsewhere; native file dialogs no longer require Tk, and completed outputs open in Explorer on Windows or Finder on macOS. ODA discovery now uses `win_exec_path` on Windows and the documented `unix_exec_path` plus macOS app/PATH candidates on macOS; development app-root resolution was corrected. Added `requirements-macos.txt`, the independent onedir `Honsen_CAD_Translator_v1.8.7_macos.spec`, an explicit missing `cryptography` runtime dependency, platform regressions, and dual-platform documentation. Python compile, four platform tests, all existing translation/API (14), queue, licence and language-assets checks, React production build, and the Apple Silicon PyInstaller build passed. `dist/Honsen CAD Translator.app` is an arm64 Mach-O app bundle (~65 MB) and remained running during a Cocoa/WebKit launch smoke check before the test instance was terminated. No ODA installation or real provider credentials were available for a macOS DWG/provider E2E; external macOS distribution still requires Developer ID signing/notarization and the mandatory real-user E2E gate.
- 2026-08-15: Integrated the user-supplied local macOS `ODAFileConverter.app` using the same adjacent-tool policy as Windows: development finds it at the repository root, and a frozen `Honsen CAD Translator.app` finds a complete `ODAFileConverter.app` beside itself (with `/Applications`, `PATH`, and `CAD_ODA_EXEC` fallbacks). The ODA bundle and `.DS_Store` files are Git-ignored and ODA is not embedded by PyInstaller. The supplied ODA 27.1 executable is arm64; ezdxf configured its exact `Contents/MacOS/ODAFileConverter` path and reported it installed. A real temporary `odafc.convert` smoke test converted R2010 DXF to ACAD2018/AC1032, preserved test text, and produced a readable output. `codesign --verify --deep --strict` reports the supplied ODA bundle has an invalid/modified signature; the bundle was not altered or re-signed, although local execution succeeded.
- 2026-08-15: Chose an official original macOS DMG as the required source for a future single-app distribution. The DMG itself will not be embedded; its signature-valid `ODAFileConverter.app` will be extracted and nested inside `Honsen CAD Translator.app` as helper code, then the outer app will be signed last and notarized. RPM/DEB are Linux-only and unsuitable; PKG is a fallback because it may include installer scripts and receipts. Redistribution remains conditional on the applicable ODA licence permitting bundling.
- 2026-08-15: Validated both official ODA 27.1 DMGs: the 54 MB arm64 and 60 MB x86_64 images pass checksums, contain the declared thin architecture, have valid nested signatures, and are accepted by Gatekeeper as Notarized Developer ID applications. Added `installer/build_macos.py`, which builds the native PyInstaller app, mounts a supplied DMG read-only to verify ODA signature/Gatekeeper/architecture, embeds that unchanged DMG at `Contents/Resources/ODAFileConverter.dmg`, then signs and verifies the outer app. Runtime mounts the internal DMG read-only and prefers its signed ODA, while retaining adjacent/applications/PATH/environment fallbacks. The direct nested-app approach was rejected because its strict signature became unstable after first launch under local ad-hoc signing; the read-only DMG design keeps the outer signature valid after a live runtime smoke check and reduces the arm64 single app from ~213 MB to ~118 MB. Live `/api/odafc-status` resolved to the temporary read-only internal ODA volume. The current Homebrew Python and native extensions are arm64-only, so the x86_64 ODA is retained for an Intel-hosted x86_64 build rather than falsely claiming a Universal 2 app.
- Current development branch: `main`; application version: 1.8.8.
- CAD translation supports Chinese, French and English in four directions. Exact full-label glossary entries bypass DeepL; non-matching text uses DeepL.
- DWG conversion requires the local `ODAFileConverter/` directory beside the application. It is a local tool directory and must not be committed.
- Packaging uses `Honsen_CAD_Translator_v1.8.8.spec`; the current Windows output executable is `dist/Honsen_CAD_Translator_v1.8.8.exe`. The local `ODAFileConverter/` directory remains beside the EXE and is not bundled or committed.
- 2026-08-08: Released 1.8.6. Sponsorship QR codes now prefetch in a daemon thread at application startup and are retained as binary cache files at `~/.cad_translator_qr_cache/wechat.bin` and `alipay.bin` (never as image-named cache files). Each entry is refreshed every seven days; an existing stale entry is served immediately while a background refresh runs. The same-origin QR endpoint reads the local cache, avoiding the modal's network round trip. Compilation, licensing tests, translation/API tests, queue/cache tests, React production build, and the PyInstaller build passed. `dist/Honsen_CAD_Translator_v1.8.6.exe` is 44,280,292 bytes; no desktop window was launched.
- 2026-08-08: Manually invalidated the local sponsorship QR cache after the remote images changed. The previous binary files were safely renamed with an `.invalidated-20260808` suffix instead of deleted; the next application start will create fresh `wechat.bin` and `alipay.bin` files from the configured URLs.
- 2026-08-08: Made the footer author name `Etienne` a normal external link to `https://github.com/etianwang`, with a no-drag region so it remains clickable in the desktop title/window shell. The frontend production build and rebuilt 1.8.6 EXE passed; no desktop window was launched.
- 2026-08-08: Implemented the local language-assets feature set in the rebuilt 1.8.6 EXE. `language_assets.py` uses standard-library SQLite at `~/.cad_translator_language_assets.sqlite3` for user terminology, translation memory, and monthly provider usage. A portable `.hcterms.json` project package is selected or created from the UI and takes precedence over user terms, bundled YAML terms, translation memory, then the selected provider. Successful provider results populate memory and local character usage; manual memory is protected from API overwrite and can be promoted to a term. The service panel reads DeepL's official usage endpoint with the current key and reports Azure F0 local character/request estimates plus the existing `403001` signal. Added `docs/LANGUAGE_ASSETS.md`, `test_language_assets.py`, native project-package dialogs, and React asset-management modal. Python compilation, licensing tests, language-asset tests, 13 translation/API tests, batch tests, frontend production build, diff check, and PyInstaller build passed. `dist/Honsen_CAD_Translator_v1.8.6.exe` is 45,208,730 bytes; no desktop window was launched.
- 2026-08-08: Exposed all four shipped YAML glossary files in the language-assets modal as a searchable read-only “内置术语” tab. Each entry can be copied into a user or project term, creating the intended higher-priority editable override without duplicating or mutating the bundled files. Added an API regression for built-in glossary export; 14 translation/API tests and the frontend build passed. Rebuilt `dist/Honsen_CAD_Translator_v1.8.6.exe` (45,210,320 bytes); no desktop window was launched.
- 2026-08-08: Further compacted the right-hand settings panel without changing the queue/log layout: settings padding, field spacing, inputs, direction chips, hints, action buttons, and dropdown option rows were reduced; closed dropdown height is now 28px and its open list is capped at 220px. Frontend build and diff check passed. Rebuilt `dist/Honsen_CAD_Translator_v1.8.6.exe` (45,210,005 bytes); no desktop window was launched.
- 2026-08-08: Released 1.8.7. Updated application/window/footer, frontend package metadata, README, test/package instructions, installer metadata, and added `Honsen_CAD_Translator_v1.8.7.spec`. Python compilation, frontend production build, diff check, and `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.8.7.spec` passed. `dist/Honsen_CAD_Translator_v1.8.7.exe` is 45,210,230 bytes; no desktop window was launched.
- 2026-08-08: Auto-update design review only; no product code changed. Recommended design is a GitHub Release stable-channel check using release assets plus a separately signed manifest and a small external updater process that waits for the app to exit before replacing the EXE. Do not rely on a mutable `latest.exe` URL or reuse the licence signing key for update signatures.
- 2026-08-08: Pushed release commit `ac36242` (`release: version 1.8.7`) on `main` to both GitHub `origin` and Gitee `gitee`. `ODAFileConverter/` remains intentionally untracked and was not pushed.
- Documentation lives in this folder. Batch queue requirements and acceptance criteria live in `BATCH_TRANSLATION_HARNESS.md`.
- Accepted batch-queue layout: left top queue (70%), left bottom live log (30%), right full-height translation settings; one target language per batch.
- 2026-08-07: Batch-queue preview and harness were updated to the 70/30 left-column split. Every completed task must append its state and decisions to this file before handoff.
- 2026-08-07: Completed batch CAD translation queue for v6.0. Added `batch_queue.py`: persisted task inputs/status/progress/logs in `~/.cad_translator_queue.json`, restores interrupted `running` tasks as `queued`, schedules in enqueue order with a global three-worker cap and a two-worker semaphore per DeepL key, serializes DWG/ODA work, supports pause/resume, removing queued items, automatic retry (up to three), and retrying completed/failed single files. Queue state and API responses deliberately exclude API keys.
- 2026-08-07: Reused `CADChineseTranslator` and `CadConversionSession`; batch tasks now accept source/DXF/DWG outputs plus optional target version. The React UI was replaced with the confirmed queue/log/settings layout and native multi-file picker. No dependencies were added; `ODAFileConverter/` was not changed.
- 2026-08-07: Validation passed: `py_compile` for translation/conversion/API/queue/bridge, `test_translation_modes.py` (3 tests), `test_batch_queue.py` (recovery, API-key exclusion, pause/resume, queued-item removal, retry), and `npm run build`. Real DWG validation at `D:\Users\Documents\翻译\钢结构楼梯加固\户外钢梯-钢楼梯结构图 H型钢基础图 2026.5.10(1).dwg` produced readable outputs for zh→fr, fr→zh, zh→en, en→zh; extracted text differed for each pass. Test outputs are in sibling `batch_queue_validation/`. No remaining implementation todo; before release, perform a visual desktop smoke test if packaging changes.
- 2026-08-07: Major-version packaging update completed: application/UI/frontend package and installer metadata are 1.7.0. Replaced the PyInstaller spec with `Honsen_CAD_Translator_v1.7.0.spec`, including the frontend build, YAML/changelog data, queue module, and desktop runtime imports. `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.7.0.spec` passed and produced `dist/Honsen_CAD_Translator_v1.7.0.exe` (42.14 MB). PyInstaller emitted only the upstream Python 3.14/Pydantic-v1 compatibility warning; a desktop launch smoke test remains recommended before distribution.
- 2026-08-07: Restored the original React visual shell after the batch-layout change removed it: `Iridescence`, vignette/noise background, Framer Motion panel and log transitions, pywebview title-bar drag region, and the minimize/close controls are retained. The queue remains in the accepted 70/30-left-column layout. `npm run build` and the 1.7.0 PyInstaller build both passed; the EXE was rebuilt with this UI.
- 2026-08-07: Simplified queue entry: selecting files now adds them directly to the queue; the staging list and separate enqueue button were removed. Output directory persists in the local app config. When no prior directory exists, the backend creates and uses `Documents/Honsen CAD output`. Verified `py_compile`, default-directory creation, React build, and rebuilt `dist/Honsen_CAD_Translator_v1.7.0.exe`.
- 2026-08-07: Corrected queue execution order: adding files only creates `queued` tasks; only the explicit **开始翻译** action begins ODA conversion and the existing `CADChineseTranslator.translate_cad_file` workflow. Queue pause/resume cannot implicitly start work. Fixed the queue log callback to accept the original translator's `message, level` call signature, restoring live SSE logs. Updated queue self-check confirms no task runs before `start()`; Python compile, frontend build, and PyInstaller rebuild passed.
- 2026-08-07: Consolidated queue controls into one stateful primary button: before execution it reads **开始翻译**, while running it reads **暂停**, and after a successful pause it reads **继续**. On queue completion it resets to **开始翻译** for the next batch. Updated the queue self-check, rebuilt the frontend and `dist/Honsen_CAD_Translator_v1.7.0.exe`.
- 2026-08-07: Added file-level progress bars and explicit percentages, moved the queue control below the DeepL API Key, and connected a shared pause event through the original `translate_cad_file` workflow. Pause takes effect before/after ODA phases and between text translations; an already-running ODA process or DeepL request completes its current atomic call before stopping. Python checks, translation tests, queue tests, and frontend build passed. EXE rebuild is currently blocked because running processes (PIDs 6572 and 26096) lock `dist/Honsen_CAD_Translator_v1.7.0.exe`; close them and rerun PyInstaller.
- 2026-08-07: Window close is now a global stop boundary. Both the pywebview closing event and the close control call queue shutdown: running tasks return to `queued` in persisted state, cancellation is signalled through the original translator checkpoints, and batch workers are daemon threads so they cannot keep the application alive after the window closes. An in-flight ODA or DeepL call is allowed to finish its atomic call, then exits without starting later work. Increased glass-panel corner radius to 28px. Compilation, translation/queue tests, frontend build, and the rebuilt 1.7.0 EXE passed.
- 2026-08-07: Queue recovery and controls refined. On restart, persisted queued work is marked resumable and waits for **继续**; it never starts automatically. Added **停止** beside the start/pause/continue control: it cancels active and pending work, marks them `cancelled`, and resets the primary control to **开始翻译**. Added **清空列表**, enabled only when no batch is running or paused; individual running tasks remain non-removable. Compilation, queue/translation tests, and frontend build passed. EXE repack is pending because running PIDs 5980 and 34072 lock the current EXE.
- 2026-08-07: Strengthened application shutdown: after queue state is synchronously saved and cancellation sent, the explicit close control calls `os._exit(0)`; the normal pywebview close path also calls shutdown then forces process exit after its event loop returns. This prevents a closed window from leaving the translator process alive. Compilation, queue self-check, and PyInstaller rebuild passed; `dist/Honsen_CAD_Translator_v1.7.0.exe` is current.
- 2026-08-07: Stopped-batch restart now deliberately applies the settings present at the next **开始翻译** click: each `cancelled` task is reset to 0% and gets the current output directory, direction, format, version, block option, and API key. In contrast, individual **重翻** preserves its task's saved settings and starts that one task directly. Queue and translation tests plus frontend build passed; EXE repack is pending because it is currently running and locked.
- 2026-08-07: Version selection now exposes the complete local ODA/ezdxf set: ACAD9, ACAD10, ACAD12, ACAD13, ACAD14, ACAD2000, ACAD2004, ACAD2007, ACAD2010, ACAD2013, and ACAD2018. Any selected output version is finalized through ODA for both DXF and DWG outputs. Restyled `select` and its options to the dark glass palette. Compilation, queue/translation tests, frontend build, and EXE rebuild passed.
- 2026-08-07: Refined the output-format and ODA-version native dropdown palette: their closed state, options, selected option, hover, and keyboard-focus states now use the blue glass palette instead of the Windows grey selection color. `npm run build` passed.
- 2026-08-07: Replaced the two native output dropdowns with a small `details`-based React menu because Windows/Chromium owns native popup rendering and prevents real transparency. The new menu uses the application's translucent blue glass treatment for its list and selected item. `npm run build` passed; EXE rebuild was not requested.
- 2026-08-07: Repacked the current frontend and application with `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.7.0.spec`. Build succeeded; `dist/Honsen_CAD_Translator_v1.7.0.exe` is 44,252,171 bytes. PyInstaller retained its known Python 3.14/Pydantic-v1 compatibility warning.
- 2026-08-07: Prepared the complete 1.7.0 batch-queue implementation, updated documentation, and current package configuration on `english` for promotion to `main`; local ODA binaries and Python caches remain excluded.
- 2026-08-07: Promoted `english` to `main` at commit `24b9a53` and pushed `main` to both GitHub (`origin`) and Gitee (`gitee`). The local `ODAFileConverter/` directory remains intentionally untracked.
- 2026-08-07: Replaced `images/demo.png` with the current 1.7.0 batch-queue screenshot and rewrote the README to document queue workflow, persistence, retry, safe concurrency, output formats/versions, ODA setup, source launch, and packaging. Documentation-only change; no rebuild required.
- 2026-08-07: UI polish: reduced the live-log heading/subtitle footprint, removed the email from the footer, and added blue, purple-magenta, and blue-pink theme controls beside the app title. Themes reuse the installed animated OGL background and update primary controls, progress, selected modes, checkboxes, and custom menus together. `npm run build` passed.
- 2026-08-07: Rebuilt the current theme-enabled application with `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.7.0.spec`; the EXE build completed successfully. The known Python 3.14/Pydantic-v1 warning remains.
- 2026-08-07: Replaced the blue-pink theme with a purple-pink-orange palette, using the supplied Grainient palette as the color reference. `npm run build` passed.
- 2026-08-07: Released build version 1.7.1: application, frontend, package metadata, installer, documentation, and spec now use `Honsen_CAD_Translator_v1.7.1`. Frontend build and `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.7.1.spec` passed; the output is `dist/Honsen_CAD_Translator_v1.7.1.exe`.
- 2026-08-07: Replaced the third theme with the distinct forest palette: deep green background, teal controls, and amber highlights. Frontend build and the 1.7.1 PyInstaller rebuild passed.
- 2026-08-07: Enhanced the default blue theme with deep blue background, cyan accents, and indigo highlights. Frontend build and the 1.7.1 PyInstaller rebuild passed.
- 2026-08-07: Enabled native pywebview window shadow even in transparent mode and added a subtle outer edge transition. Frontend build passed; rebuilding the default EXE is pending because active `Honsen_CAD_Translator_v1.7.1.exe` processes lock it.
- 2026-08-07: After the running application closed, the native-window-shadow build completed successfully with `Honsen_CAD_Translator_v1.7.1.spec`.
- 2026-08-08: Read-only architecture and defect review completed before adding Azure/Google translation providers. Current verification passed: Python compilation for translation/conversion/API/queue/bridge modules, `test_translation_modes.py` (3 tests), `test_batch_queue.py`, and `frontend/npm run build`. No source changes were made. Key findings: provider handling, API models, UI, queue credentials, and rate limiting are DeepL-specific; `translate_text` logs provider failures and returns the source text so a failed batch can be marked succeeded; queue retry sleeps while holding its state lock; API keys are stored as plaintext in the local JSON config. Recommended next change is a provider adapter boundary that keeps the existing DeepL adapter and CAD/glossary/ODA workflow unchanged, then adds Azure Translator v3 and Google Cloud Translation Basic v2 adapters with provider-specific credentials and tests.
- 2026-08-08: Fixed non-secret defects before provider integration. A DeepL translation exception now propagates as a failed CAD task instead of saving source text and reporting success; batch retry backoff now waits outside the queue lock and is immediately cancellable; single-file API validates translation mode before starting work. Added regression checks for provider failure propagation, invalid single-file mode rejection, and stopping during retry backoff. Python compilation, five translation tests, the batch self-check, and the React production build passed. DeepL key storage/calling behavior was deliberately unchanged. Next planned work remains the Azure Translator v3 plus Google Cloud Translation Basic v2 adapter layer.
- 2026-08-08: Added Azure Translator Text v3 support limited to the Azure F0 free tier workflow; Google Cloud was intentionally not added. The React settings now select DeepL or Azure Translator F0, capture Azure Key and optional Region, and retain the existing DeepL path. `azure_translator.py` uses only the standard library, maps the four application directions to Azure `zh-Hans`/`fr`/`en` codes, and sends the v3 REST request. Azure error `403001` becomes a non-retryable `AzureFreeQuotaExceededError`: the task fails with an instruction to wait for the next monthly reset or upgrade, with no retry attempt. Provider and Azure region persist with queue tasks, while all task API keys remain transient and excluded from queue state. Documentation and packaging hidden imports were updated. Verification passed: Python compilation, eight translation/API Azure tests, batch queue self-check, and React production build. No real Azure request was made, so F0 credentials and live quota behavior still require a user smoke test.
- 2026-08-08: Desktop visual smoke preview launched from `main.py`; the default DeepL-selected batch queue rendered correctly with the expected 70/30 left-column layout and right-side translation settings. A preview screenshot was captured outside the repository for handoff. No project source or configuration was changed by this preview.
- 2026-08-08: Compacted the right-side translation settings panel for the Azure F0 fields: panel padding, field gaps, inputs, dropdowns, direction chips, hints, and queue-control buttons now use denser settings-only dimensions, leaving queue/log layout unchanged. `frontend/npm run build` passed. A live desktop preview launch did not alter project configuration.
- 2026-08-08: Rebuilt the compact-settings/Azure build with `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.7.1.spec`; the executable was updated at `dist/Honsen_CAD_Translator_v1.7.1.exe` (44,257,931 bytes) and successfully launched for a desktop smoke check. The program is currently running as the launched EXE process.
- 2026-08-08: Released version 1.8.0 for the Azure F0 and compact-settings work. Updated application title/version, frontend package metadata/footer, README, test packaging instructions, PyInstaller spec, and Inno Setup script/build references; renamed the spec and installer script to `Honsen_CAD_Translator_v1.8.0.*`. Python compilation, eight translation/API tests, queue self-check, and frontend build passed. `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.8.0.spec` produced `dist/Honsen_CAD_Translator_v1.8.0.exe` (44,257,602 bytes); only the known Python 3.14/Pydantic-v1 warning was emitted.
- 2026-08-08: Completed a read-only v1.8.0 implementation review; no product source was changed. Python compilation, eight translation/Azure tests, queue self-check, and the frontend production build passed. Priority follow-ups identified: make queue/config persistence atomic and recover corrupt files; make entity write-back failures fail the task instead of saving a partially translated drawing as success; preserve Azure settings when the shipped `--legacy` Tk UI saves its DeepL key; reject or explicitly choose a safe policy for unrecognised DWG version signatures instead of silently outputting ACAD2010. Secondary follow-ups: classify Azure 400/401/non-quota 403 as non-retryable, remove the Baidu preflight dependency, bound/lock SSE log subscriptions, and avoid a second desktop instance attaching to an old server on fixed port 8765.
- 2026-08-08: Fixed three Azure/service reliability issues without altering DeepL translation behaviour. Azure HTTP 400, 401, and non-quota 403 errors are now non-retryable (while 429 and transient failures still retry); the F0 `403001` handling remains non-retryable. Removed the Baidu connectivity preflight from the web single-file flow so the selected translation service is the connectivity authority. Recovered queue tasks now resolve their limiter key from each task's provider, so Azure tasks use the saved Azure key rather than the DeepL key. Added regression coverage for non-retryable Azure request/auth errors and recovered Azure queue key resolution. Python compilation, nine translation tests, queue self-check, and React production build passed.
- 2026-08-08: Performed a live Azure Translator F0 smoke test with a user-provided key held only in the current process environment (not written to project files or application config). The global endpoint accepted the key without a Region header. A real 111,530-byte Chinese DWG with 75 extracted non-block text entities completed Chinese-to-English conversion; the separate temporary output DWG was readable through ODA/ezdxf, retained 75 entities, changed 47 positions, and had DWG signature `AC1032`. The temporary output is `C:\\Users\\THISPC~1\\AppData\\Local\\Temp\\cad_azure_dwg_smoke_fjvm0ci3\\azure_zh_to_en_smoke.dwg`.
- 2026-08-08: Released executable version 1.8.1. Updated application and frontend version labels, package metadata, README, test packaging references, and added `Honsen_CAD_Translator_v1.8.1.spec`. Python compilation, nine translation/Azure tests, batch queue self-check, and React production build passed. `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.8.1.spec` produced `dist/Honsen_CAD_Translator_v1.8.1.exe` (44,257,464 bytes); `dist/ODAFileConverter/ODAFileConverter.exe` is present beside it. The known Python 3.14/Pydantic-v1 compatibility warning and optional `pycparser` hidden-import warnings were emitted, with no build failure.
- 2026-08-08: Corrected queue configuration timing for Azure/DeepL switching. Adding files now persists only file paths, order, and task state; it does not send or save output location, format/version, language direction, provider, region, or API key. Pressing the main start button validates the current service Key and ODA availability, then applies the current settings to every queued, retrying, cancelled, or failed task. A single-task retry retains the settings from the previous start. Updated queue acceptance documentation and regression checks. Python compilation, nine translation/Azure tests, queue self-check, and React production build passed. Rebuilt `dist/Honsen_CAD_Translator_v1.8.1.exe` (44,257,089 bytes); ODA remains adjacent.
- 2026-08-08: Released executable version 1.8.2 for the deferred queue-settings workflow. Updated application/frontend version labels, package metadata, README, test packaging references, and added `Honsen_CAD_Translator_v1.8.2.spec`. Python compilation, nine translation/Azure tests, queue self-check, and React production build passed. `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.8.2.spec` produced `dist/Honsen_CAD_Translator_v1.8.2.exe` (44,257,939 bytes); ODA remains adjacent. The known Python 3.14/Pydantic-v1 and optional `pycparser` warnings did not block the build.
- 2026-08-08: Released executable version 1.8.3 for the expanded construction/electrical/water terminology. Updated application and frontend labels, package metadata, README, test packaging references, and added `Honsen_CAD_Translator_v1.8.3.spec`. The bare French `alimentation` is now resolved by drawing layer (water/plumbing → “供水”; electrical → “供电”), and cache entries include layer context. Frontend production build, Python compilation, nine translation/Azure tests, and batch queue self-check passed. `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.8.3.spec` produced `dist/Honsen_CAD_Translator_v1.8.3.exe` (44,260,000 bytes). The known Python 3.14/Pydantic-v1 warning did not block the build; desktop smoke launch was deferred because 1.8.2 is running.
- 2026-08-08: Added DXF/DWG drag-and-drop to the batch queue. The React queue panel accepts files dropped from Explorer, shows a drop target, uploads them to the app’s persistent dropped-files directory, then reuses the existing add-to-queue endpoint. This avoids relying on unavailable browser file paths and preserves queued dropped files across restart. Invalid extensions are rejected. Added a dropped-file persistence check; Python compilation, queue self-check, nine translation/Azure tests, and frontend production build passed. The 1.8.3 EXE must be rebuilt after this change.
- 2026-08-08: Rebuilt `dist/Honsen_CAD_Translator_v1.8.3.exe` with DXF/DWG drag-and-drop support (44,262,124 bytes). PyInstaller emitted only the known Python 3.14/Pydantic-v1 warning and optional `pycparser` hidden-import warnings; neither blocked the build.
- 2026-08-08: Added session-log export. The service keeps the complete in-memory session log (clearing the UI clears it too); the desktop UI opens a native save dialog and writes UTF-8-with-BOM text for Notepad compatibility. A browser-download fallback remains available for web preview. Added an export-file regression check; Python compilation, batch queue self-check, nine translation/Azure tests, and frontend production build passed. The 1.8.3 EXE must be rebuilt after this change.
- 2026-08-08: Rebuilt `dist/Honsen_CAD_Translator_v1.8.3.exe` with session-log export (44,262,029 bytes). PyInstaller completed with only the known Python 3.14/Pydantic-v1 warning and optional `pycparser` hidden-import warnings.
- 2026-08-08: Refined log and completed-task actions. The export/clear buttons are a right-aligned action group with export left of clear. Backend session logs now use an in-memory `deque(maxlen=5000)`: newest logs are retained, oldest are automatically discarded, nothing is persisted, and process exit releases all logs. Successful tasks now expose a Windows Explorer “locate file” action that selects their output. Added a bounded-log regression check; Python compilation, queue self-check, nine translation/Azure tests, and frontend production build passed. The 1.8.3 EXE must be rebuilt after this change.
- 2026-08-08: Rebuilt `dist/Honsen_CAD_Translator_v1.8.3.exe` with bounded session logs and output-file location support (44,262,769 bytes). PyInstaller completed with only the known Python 3.14/Pydantic-v1 warning and optional `pycparser` hidden-import warnings.
- 2026-08-08: Reduced the closed height of the three settings-panel custom dropdowns (output format, output version, translation provider) from 36px to 32px, with tighter vertical padding; dropdown option lists and other controls are unchanged. Frontend production build passed. The 1.8.3 EXE must be rebuilt after this change.
- 2026-08-08: Rebuilt `dist/Honsen_CAD_Translator_v1.8.3.exe` with the compact settings dropdowns (44,262,504 bytes). PyInstaller completed with only the known Python 3.14/Pydantic-v1 warning and optional `pycparser` hidden-import warnings.
- 2026-08-08: Released version 1.8.5. Updated application/UI labels, frontend package metadata, README, packaging test references, and added `Honsen_CAD_Translator_v1.8.5.spec`. Frontend production build, Python compilation, batch queue self-check, and nine translation/Azure tests passed. `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.8.5.spec` produced `dist/Honsen_CAD_Translator_v1.8.5.exe` (44,262,765 bytes). The known Python 3.14/Pydantic-v1 warning and optional `pycparser` warnings did not block the build.
- 2026-08-08: Added a mandatory real-user E2E release gate to `docs/TEST_RULES.md`: all four language directions must be tested through real desktop clicks with both Azure and DeepL (eight runs), randomly selected format/version, readable translated output, successful log export, and Explorer file location. Current environment has Azure configured and ODA available but no DeepL Key, so this gate is not yet passed and no claim of two-provider E2E success is valid.
- 2026-08-08: The mandatory desktop E2E gate now additionally requires credential isolation: DeepL runs use only a DeepL Key with Azure empty, and Azure runs use only an Azure Key with DeepL empty. An attempt to begin the isolated interactive test was stopped at the user's request because it interfered with the active desktop; no E2E pass is claimed and it must be run only when the desktop is available.
- 2026-08-08: In an isolated local web-server profile, eight live translation runs passed: Azure and DeepL each completed all four directions with the randomly selected format/version matrix, only the active provider's credential present, no task-log errors, existing outputs, readable DXF/DWG round trips (DWG opened through ODA then ezdxf), and expected translated text. Browser automation confirmed the visible direction/format/version controls and a DeepL start-button run, but its security proxy cannot type password fields, synthesize a browser `File`/drop event, or verify a browser download. Therefore the mandatory **desktop** E2E gate remains unpassed: native log-save and Explorer locate actions still need a non-disruptive desktop-session confirmation.
- 2026-08-08: Completed a read-only whole-project audit. Python compilation, `test_translation_modes.py` (9 tests), `test_batch_queue.py`, and the React production build passed. No implementation was changed. Priority findings for a future patch: loopback FastAPI exposes configuration (including credentials) under permissive CORS; entity write-back errors are swallowed and can leave a task marked successful; simultaneous same-stem inputs can choose the same output path; fixed port 8765 can attach a second window to an older process. Secondary findings: queue/config writes are non-atomic and corrupt state is silently discarded, persisted completed-task logs and dropped-file copies have no retention policy, SSE subscriber queues are unbounded, output-version input is not validated server-side, the legacy UI overwrites Azure configuration, and development resource lookup depends on the current working directory.
- 2026-08-08: Fixed the four priority audit findings without changing provider translation behaviour. Removed permissive CORS because the desktop UI and Vite development proxy are same-origin; write-back now re-raises and fails the task instead of silently saving untranslated entities; concurrent same-stem tasks reserve unique in-memory output paths (and retry/new batch settings reset that reservation); Windows startup now uses a named mutex and confirms that the newly created Uvicorn server, not an older process, owns port 8765. Added regressions for no CORS middleware, propagated write-back failure, unique output reservation, and the mutex guard. Python compilation, 11 translation tests, batch queue checks, frontend production build, and the Windows guard self-check passed.
- 2026-08-08: Completed the remaining non-key reliability fixes. Queue and config JSON writes are now flushed and atomically replaced (with brief Windows-lock retry); unreadable JSON is quarantined as a timestamped `.corrupt-*` file instead of silently overwriting it. Finished-task history is capped at 100 entries, unreferenced drag-drop copies expire after 30 days, and every SSE subscriber has a locked 500-event queue so slow clients cannot grow memory or block translation. DXF/DWG delivery now writes a same-directory temporary target and atomically publishes only a completed output. The API rejects unsupported ODA versions, the legacy DeepL-key save merges rather than removes Azure settings, and development resource lookup uses the module directory rather than the launch directory. API-key storage policy was deliberately left unchanged. Python compilation, 13 translation/API tests, queue persistence/recovery tests (including corrupt-file recovery and atomic output), and the React production build passed. Packaging for v1.8.5 is the remaining release step.
- 2026-08-08: Rebuilt the completed reliability patch as `dist/Honsen_CAD_Translator_v1.8.5.exe` (44,268,314 bytes) with `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.8.5.spec`. The build completed successfully; PyInstaller emitted only its known Python 3.14/Pydantic-v1 compatibility warning and optional missing `pycparser` hidden-import warnings. No desktop window was started, so the native desktop-only E2E gate remains pending.
- 2026-08-08: Added paid licensing. A first successful network-time sync starts a 30-day trial; every API call is gated by a cached HTTPS Date sync, so expiry, time rollback, or loss of network-time access blocks translation until valid activation. Signed Ed25519 activation codes contain `expires_on` and plan metadata and are verified using only the packaged public key. The vendor-only issuer stores its generated private key outside the repository at `Documents/HonsenCADLicenseIssuer/license_private_key.pem`; it must never be distributed. The frontend shows a blocking activation page, while legacy mode displays an activation requirement. `test_license.py`, compilation, 13 translation/API tests, queue tests, and frontend build passed. `pyinstaller --clean --noconfirm Honsen_CAD_Translator_v1.8.5.spec` rebuilt `dist/Honsen_CAD_Translator_v1.8.5.exe` (44,277,041 bytes); only the known Python 3.14/Pydantic-v1 and optional `pycparser` warnings appeared.
- 2026-08-08: Removed the `--legacy` Tkinter launch path; passing that switch now exits with a removal message. Added the code-level `LICENSE_ENFORCEMENT_ENABLED` switch in `license_manager.py`; it defaults to `False`, bypassing all trial, activation, and network-time activity for the non-commercial ODA build. A separately licensed build sets it to `True` before packaging, enabling the existing signed 30-day trial flow. Updated licensing/test documentation and added a regression asserting that disabled licensing makes no network-time request. Compilation, licensing, translation/API, queue, and frontend-build checks passed. Rebuilt `dist/Honsen_CAD_Translator_v1.8.5.exe` (44,275,965 bytes); only the known PyInstaller warnings appeared.
- 2026-08-08: Added the title-bar support/licence action immediately left of the theme buttons. It reads the same code-level licensing switch: enabled builds show “购买许可” and current trial/licence status; disabled builds show “赞助作者” and externally hosted WeChat/Alipay QR images. The two QR URL constants are blank by default and live in `license_manager.py`; recommended deployment is fixed Cloudflare R2 object keys behind a custom domain, replacing the same key and purging/expiring CDN cache after updates. `docs/LICENSING.md` contains the exact configuration. Compilation, licensing, translation/API, queue, frontend build, and diff check passed. Rebuilt `dist/Honsen_CAD_Translator_v1.8.5.exe` (44,277,837 bytes); no desktop window was started.
- 2026-08-08: Configured sponsorship QR links to the supplied Gitee raw fixed filenames: `qr_wx.jpg` (WeChat) and `qr_ali.jpg` (Alipay). Both returned HTTP 200, `image/jpeg`, and `Cache-Control: public, max-age=60`; replacing the same repository files therefore updates existing clients within roughly one minute. Python compilation, licensing tests, frontend build, and diff check passed. Rebuilt `dist/Honsen_CAD_Translator_v1.8.5.exe` (44,277,827 bytes); no desktop window was started.
- 2026-08-08: Completed the strict paid-build purchase flow. “购买许可” always shows current trial/licence expiry and plan together with an activation input and button, allowing replacement with a later code before expiry. When licensing is enabled and the status is not usable, the purchase modal opens automatically after startup, blocks the UI, and its close action invokes the native application close; only successful activation removes the block. Backend API gating remains the enforcement boundary. Added signed-code renewal coverage. Compilation, licensing, translation/API, queue, frontend build, and diff check passed. Rebuilt `dist/Honsen_CAD_Translator_v1.8.5.exe` (44,277,209 bytes); no desktop window was started.
- 2026-08-08: Fixed sponsorship QR rendering in embedded WebView by fetching the two fixed Gitee URLs through a same-origin `/api/support/qrcode/{wechat|alipay}` proxy. The proxy accepts only the two configured static URLs, verifies an `image/*` response, and disables local caching so an upstream replacement is obtained on the next dialog open. Direct checks returned valid JPEG payloads: 74,066 bytes for WeChat and 166,976 for Alipay. Python compilation, licensing tests, frontend build, and diff check passed. Rebuilt `dist/Honsen_CAD_Translator_v1.8.5.exe` (44,278,021 bytes); no desktop window was started.
- 2026-08-08: Resolved the ambiguous French label `alimentation` by layer, rather than forcing one glossary translation: water/plumbing layers containing `eau`, `plomb`, `sanit`, `hydrau`, or `aep` map to “供水”; electrical layers containing `elec`, `élec`, `cfo`, `cfa`, `power`, or `courant` map to “供电”. The bare term no longer has a global exact-match entry. Added exact water-supply phrases and made the translation cache layer-aware so results cannot leak between disciplines. Python compilation, YAML parsing, and all nine translation/Azure tests passed. Repackaging remains pending until the running 1.8.2 processes close.
- 2026-08-08: Expanded the exact-match electrical glossary: French `alimentation` now resolves to “供电” rather than the generic “供应”, with electrical, main, and emergency power-supply variants. Matching Chinese-to-French and Chinese-to-English reverse entries were added. YAML parsing and all nine translation/Azure regression tests passed. The 1.8.2 EXE still needs repackaging after its running instances are closed.
- 2026-08-08: Read-only terminology diagnosis: glossary entries are loaded from each `translation_context*.yaml` file and applied only to complete, cleaned, case-insensitive entity labels before any provider request. `ouverture` and `ouverture de mur` have no French-to-Chinese entry, while `réservation` maps to `预留洞`; both ouverture labels therefore fall through to Azure/DeepL, which can choose the non-CAD meaning `开幕`. The existing context-hint method only logs a hint and is not sent to either provider. No source or terminology changes were made in this diagnosis.
- 2026-08-08: Expanded the exact-match CAD glossary for opening and door/window construction labels in Chinese↔French and Chinese↔English. French-to-Chinese now includes `ouverture`→`开洞` plus wall/slab/stair variants, `percement`, `trémie`, door/window openings, doors, windows, French windows, and lintels; equivalent reverse French and English terms were added. Regression checks verify representative case-insensitive matches without a provider call; all four YAML files parse, nine translation/Azure tests and queue self-check pass. Repackaging the same 1.8.2 EXE is pending because two running `Honsen_CAD_Translator_v1.8.2.exe` processes lock its target file.
- 2026-08-08: 为 1.8.0 至 1.8.7 整理了面向用户的发布说明草案；未改动产品代码或发布包。
- 2026-08-08: Repository cleanup: removed obsolete 1.8.0, 1.8.1, 1.8.2, 1.8.3, and 1.8.6 PyInstaller specs. Renamed the current Inno Setup script to `installer/Honsen_CAD_Translator_v1.8.7.iss` and updated the installer build script to call it. No Python source file is unreachable: every module is used by the startup/import chain, a test, or the documented vendor-only licence issuer workflow; no `/archive` code move was warranted.
- 2026-08-08: Pushed repository-cleanup commit `97246c8` (`chore: remove obsolete packaging configs`) on `main` to GitHub `origin` and Gitee `gitee`. `ODAFileConverter/` remains intentionally untracked and excluded.
- 2026-08-08: Repository-layout review only; no move performed. Recommended a behavior-preserving package split: `backend/` for translation, API, queue, conversion, storage, language assets, providers and licensing; `desktop/` for the pywebview launcher and native bridge; `tools/` for the vendor-only licence issuer; and `tests/` for test files. Keep a small root `run.py` as the packaging entry point. Pending user approval before moving files and updating imports/PyInstaller.
- 2026-08-08: Completed the approved repository package split without changing application behaviour. Backend modules now live under `backend/` (with Azure under `backend/providers/`), desktop shell modules under `desktop/`, the vendor-only issuer under `tools/`, and tests under `tests/`; root `run.py` is the sole source and PyInstaller entry point. Updated imports, resource lookup, docs, tests, and PyInstaller hidden imports. `python -m compileall -q backend desktop tools run.py`, all four regression modules, frontend build, `python run.py --legacy`, and the clean v1.8.7 PyInstaller build passed. The rebuilt EXE was not launched.
- 2026-08-08: Moved all shipped YAML glossary and correction files into `glossaries/`. Updated backend resource references, the built-in glossary API list, PyInstaller data placement, and README. Terminology precedence and matching behaviour remain unchanged. Compilation, 14 translation/API tests, language-assets checks, frontend build, and the clean PyInstaller build passed; the packaged EXE contains the glossary files under `glossaries/` and was not launched.
- 2026-08-08: Pushed `cd76e75` (`refactor: organize application modules and glossaries`) on `main` to GitHub `origin` and Gitee `gitee`. It includes the user-requested replacement installer script `installer/Honsen_DrawTranslate_Setup.iss`; `ODAFileConverter/` remains intentionally untracked.
